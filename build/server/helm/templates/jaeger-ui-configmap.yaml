apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    io.service.name: kusto-jaeger
  name: jaeger-ui-config
  namespace: {{ .Release.Namespace }}
data:
  jaeger-ui.json: |
    {
      "monitor": {
        "menuEnabled": {{ .Values.metrics.enabled | default false }}
      }
    }
